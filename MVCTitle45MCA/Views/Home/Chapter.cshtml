@model MVCTitle45MCA.ViewModels.Title45ViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row">
        <div class="col-sm">

        </div>
        <div class="col-sm">
            @using (Html.BeginForm("Part", "Home"))
            {
                <div>
                    @Html.DropDownListFor(m => m.SelectedChapter, new SelectList(Model.SelectListChapter, "Value", "Text"), "Select Chapter")
                </div>
                <input type="submit" value="submit" />
            }
        </div>
        <div class="col-sm">

        </div>

    </div>
</div>

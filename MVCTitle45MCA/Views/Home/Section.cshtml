@model MVCTitle45MCA.ViewModels.Title45ViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row">
        <div class="col-sm">

        </div>
        <div class="col-sm">
            @using (Html.BeginForm("Content", "Home"))
            {
                <div>
                    @Html.DropDownListFor(m => m.SelectedSection, new SelectList(Model.SelectListSection, "Value", "Text"), "Select Section")
                </div>
                <input type="submit" value="submit" />
            }
        </div>
        <div class="col-sm">

        </div>

    </div>
